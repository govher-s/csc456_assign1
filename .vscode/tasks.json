{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "build shell",
      "command": "/usr/bin/g++",
      "args": [
        "-g",
        "shell.cpp",
        "-o",
        "${workspaceFolder}/shell"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": ["$gcc"],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
